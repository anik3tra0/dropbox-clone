%h1 Listing Uploads
%table
  %thead
    %tr
      %th Upload data
      %th User
      %th{:colspan => "3"}
  %tbody
    - @uploads.each do |upload|
      %tr
        %td= image_tag upload.upload_url, class: 'rounded image-dimensions'
        %td= upload.user.name || upload.user.email
        %td= link_to 'Show', upload
        %td= link_to 'Edit', edit_upload_path(upload)
        %td= link_to 'Destroy', upload, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Upload', new_upload_path, class: 'btn btn-secondary'
