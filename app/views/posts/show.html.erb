<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<% @uploads.each do |upload| %>
	<%= image_tag upload.upload_url, size: "300x250", alt: upload.file_name %>
<% end %>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
